---
- name: Shutdown hosts gracefully
  hosts: all
  become: true  # Necessary for permission to shut down the system
  tasks:
    - name: Unconditionally shutdown the machine
      community.general.shutdown:
